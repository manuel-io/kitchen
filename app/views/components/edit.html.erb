<nav>
  <h3><%= @component.recipe.title %></h3>
  <h4><%= @component.title %></h4>
  <%= link_to 'Back', @component %>
</nav>

<aside>
  <% if @component.errors.any? %>
  <section class='error'>
    <% @component.errors.full_messages.each do |message| %>
      <p><%= message %><p>
    <% end %>
      </ul>
  </section>
  <% end %>
</aside>

<article>
  <h2>Editing Component</h2>
  <%= render 'form', component: @component %>
</article>
