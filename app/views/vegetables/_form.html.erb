<%= form_with(model: vegetable, local: true) do |form| %>
  <% if vegetable.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(vegetable.errors.count, "error") %> prohibited this vegetable from being saved:</h2>

      <ul>
      <% vegetable.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :vegetable_title %>
  </div>

  <div class="field">
    <%= form.label :amount %>
    <%= form.text_field :amount, id: :vegetable_amount %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.text_field :price, id: :vegetable_price %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
