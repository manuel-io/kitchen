<% @recipes.each do |value| %>
  <% if value.sources.length > 0 %>
* <%= value.title %>: <% value.sources[0...-1].each do |source| %>[<%= source.title -%>](<%= source.url %>), <% end %><% source = value.sources.last %>[<%= source.title %>](<%= source.url %>)
  <% end %>
<% end %>
